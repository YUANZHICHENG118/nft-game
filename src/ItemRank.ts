import { ui } from "./ui/layaMaxUI";

export default class ItemRank extends ui.ItemEmailUI{
    
    constructor() { super(); }
    
    onEnable(): void {
    }

    onDisable(): void {
    }
    setItem(itemData:any):void{

    }
}